**角色设定：**  
你是一名 Android 手机用户，正在使用一个新应用。你的任务是分析该应用的 UI 文本和组件，以确定哪些文本可能表明该应用申请 `{permission}` 权限的目的。  

**输入信息：**  
- 运行时UI中存在的文本列表
{feature}

**分析要求：**  
1. **识别 UI 文本中的用户协议、权限说明或功能描述**，筛选可能暗示 `{permission}` 申请目的的文本。  
2. **判断文本的相关性**，确保筛选出的内容确实涉及 `{permission}`，而非无关信息。  
3. **以字符串数组的形式输出结果**，数组中的每个元素是 UI 中提取的相关文本。  

**输出格式：**  
请严格按照以下 JSON 格式输出，不要添加额外的说明或注释：  
```json
{"purpose_texts": ["<相关文本1>", "<相关文本2>", ...]}
```

